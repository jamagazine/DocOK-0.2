import json
import asyncio

async def process_items(extracted_text: str):
    """
    Stub for processing invoice items/table data.
    In the future, this will use LLM or rule-based parsing to extract line items.
    """
    # For now, return an empty list as a placeholder
    return []
